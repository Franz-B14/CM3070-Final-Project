/*
  responder_secrets.example.h

  Copy this file to:
      responder_secrets.h

  Then replace the placeholder below with the same gateway-to-responder
  HMAC secret configured on the Raspberry Pi gateway.

  responder_secrets.h is intentionally excluded from Git.
*/

#pragma once

#define RESPONDER_GATEWAY_KEY "REPLACE_WITH_GATEWAY_SECRET_KEY"
